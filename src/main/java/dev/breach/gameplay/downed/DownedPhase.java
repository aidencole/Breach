package dev.breach.gameplay.downed;

public enum DownedPhase {
	/** Player is in the challenge dimension awaiting self-revive or rescue. */
	CHALLENGE,
	/** Player is being field-revived or placed in bed (transient). */
	REVIVING
}
